---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: cloudflareaccessgroups.cloudflare.zelic.io
spec:
  group: cloudflare.zelic.io
  names:
    kind: CloudflareAccessGroup
    listKind: CloudflareAccessGroupList
    plural: cloudflareaccessgroups
    singular: cloudflareaccessgroup
  scope: Namespaced
  versions:
  - name: v4alpha1
    schema:
      openAPIV3Schema:
        description: CloudflareAccessGroup is the Schema for the cloudflareaccessgroups
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CloudflareAccessGroupSpec defines the desired state of CloudflareAccessGroup.
            properties:
              exclude:
                description: Rules evaluated with a NOT logical operator. To match
                  the policy, a user cannot meet any of the Exclude rules.
                properties:
                  accessGroupRefs:
                    description: Would match access groups refs by {name} or {namespace/name}
                      of [CloudflareAccessGroup]
                    items:
                      type: string
                    type: array
                  anyAccessServiceToken:
                    description: Would be a match if using any valid service token
                    type: boolean
                  commonNames:
                    description: Matches Certificates CNs
                    items:
                      type: string
                    type: array
                  countries:
                    description: Matches Country IDs (https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)
                    items:
                      type: string
                    type: array
                  emailDomains:
                    description: Matches specific email domains
                    items:
                      type: string
                    type: array
                  emails:
                    description: Matches specific email adresses
                    items:
                      type: string
                    type: array
                  everyone:
                    description: Allow Everyone; would always be a match
                    type: boolean
                  githubOrganizations:
                    description: Matches Github Organizations
                    items:
                      properties:
                        identityProviderId:
                          description: An Identity Provider UUID of a "github" type
                            Identity Provider (https://developers.cloudflare.com/api/resources/zero_trust/subresources/identity_providers/methods/get/)
                          type: string
                        name:
                          description: The name of the organization.
                          type: string
                        team:
                          description: The name of the team, if restricting to it.
                          type: string
                      required:
                      - identityProviderId
                      - name
                      type: object
                    type: array
                  googleGroups:
                    description: Matches Google Groups
                    items:
                      properties:
                        email:
                          description: Google group email
                          type: string
                        identityProviderId:
                          description: An Identity Provider UUID of a "google" type
                            Identity Provider (https://developers.cloudflare.com/api/resources/zero_trust/subresources/identity_providers/methods/get/)
                          type: string
                      required:
                      - email
                      - identityProviderId
                      type: object
                    type: array
                  ipRanges:
                    description: Matches IP CIDR blocks (https://www.ipaddressguide.com/cidr)
                    items:
                      type: string
                    type: array
                  loginMethods:
                    description: Matches on Identity Provider UUIDs (https://developers.cloudflare.com/api/resources/zero_trust/subresources/identity_providers/methods/get/)
                    items:
                      type: string
                    type: array
                  oktaGroups:
                    description: Matches Okta Groups
                    items:
                      properties:
                        identityProviderId:
                          description: An Identity Provider UUID of a "okta" type
                            Identity Provider (https://developers.cloudflare.com/api/resources/zero_trust/subresources/identity_providers/methods/get/)
                          type: string
                        name:
                          description: Name of the Okta Group
                          type: string
                      required:
                      - identityProviderId
                      - name
                      type: object
                    type: array
                  samlGroups:
                    description: Matches SAML Groups
                    items:
                      properties:
                        identityProviderId:
                          description: An Identity Provider UUID of a "saml" type
                            Identity Provider (https://developers.cloudflare.com/api/resources/zero_trust/subresources/identity_providers/methods/get/)
                          type: string
                        name:
                          description: Name of the SAML group
                          type: string
                        value:
                          description: Value of the SAML group
                          type: string
                      required:
                      - identityProviderId
                      - name
                      - value
                      type: object
                    type: array
                  serviceTokenRefs:
                    description: Would match access groups refs by {name} or {namespace/name}
                      of [CloudflareServiceToken]
                    items:
                      type: string
                    type: array
                  validCertificate:
                    description: Would be a match if using any valid certificate
                    type: boolean
                type: object
              include:
                description: Rules evaluated with an OR logical operator. A user needs
                  to meet only one of the Include rules.
                properties:
                  accessGroupRefs:
                    description: Would match access groups refs by {name} or {namespace/name}
                      of [CloudflareAccessGroup]
                    items:
                      type: string
                    type: array
                  anyAccessServiceToken:
                    description: Would be a match if using any valid service token
                    type: boolean
                  commonNames:
                    description: Matches Certificates CNs
                    items:
                      type: string
                    type: array
                  countries:
                    description: Matches Country IDs (https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)
                    items:
                      type: string
                    type: array
                  emailDomains:
                    description: Matches specific email domains
                    items:
                      type: string
                    type: array
                  emails:
                    description: Matches specific email adresses
                    items:
                      type: string
                    type: array
                  everyone:
                    description: Allow Everyone; would always be a match
                    type: boolean
                  githubOrganizations:
                    description: Matches Github Organizations
                    items:
                      properties:
                        identityProviderId:
                          description: An Identity Provider UUID of a "github" type
                            Identity Provider (https://developers.cloudflare.com/api/resources/zero_trust/subresources/identity_providers/methods/get/)
                          type: string
                        name:
                          description: The name of the organization.
                          type: string
                        team:
                          description: The name of the team, if restricting to it.
                          type: string
                      required:
                      - identityProviderId
                      - name
                      type: object
                    type: array
                  googleGroups:
                    description: Matches Google Groups
                    items:
                      properties:
                        email:
                          description: Google group email
                          type: string
                        identityProviderId:
                          description: An Identity Provider UUID of a "google" type
                            Identity Provider (https://developers.cloudflare.com/api/resources/zero_trust/subresources/identity_providers/methods/get/)
                          type: string
                      required:
                      - email
                      - identityProviderId
                      type: object
                    type: array
                  ipRanges:
                    description: Matches IP CIDR blocks (https://www.ipaddressguide.com/cidr)
                    items:
                      type: string
                    type: array
                  loginMethods:
                    description: Matches on Identity Provider UUIDs (https://developers.cloudflare.com/api/resources/zero_trust/subresources/identity_providers/methods/get/)
                    items:
                      type: string
                    type: array
                  oktaGroups:
                    description: Matches Okta Groups
                    items:
                      properties:
                        identityProviderId:
                          description: An Identity Provider UUID of a "okta" type
                            Identity Provider (https://developers.cloudflare.com/api/resources/zero_trust/subresources/identity_providers/methods/get/)
                          type: string
                        name:
                          description: Name of the Okta Group
                          type: string
                      required:
                      - identityProviderId
                      - name
                      type: object
                    type: array
                  samlGroups:
                    description: Matches SAML Groups
                    items:
                      properties:
                        identityProviderId:
                          description: An Identity Provider UUID of a "saml" type
                            Identity Provider (https://developers.cloudflare.com/api/resources/zero_trust/subresources/identity_providers/methods/get/)
                          type: string
                        name:
                          description: Name of the SAML group
                          type: string
                        value:
                          description: Value of the SAML group
                          type: string
                      required:
                      - identityProviderId
                      - name
                      - value
                      type: object
                    type: array
                  serviceTokenRefs:
                    description: Would match access groups refs by {name} or {namespace/name}
                      of [CloudflareServiceToken]
                    items:
                      type: string
                    type: array
                  validCertificate:
                    description: Would be a match if using any valid certificate
                    type: boolean
                type: object
              name:
                description: Name of the Cloudflare Access Group. Must be unique within
                  a CloudFlare Account
                type: string
              require:
                description: Rules evaluated with an AND logical operator. To match
                  the policy, a user must meet all the Require rules.
                properties:
                  accessGroupRefs:
                    description: Would match access groups refs by {name} or {namespace/name}
                      of [CloudflareAccessGroup]
                    items:
                      type: string
                    type: array
                  anyAccessServiceToken:
                    description: Would be a match if using any valid service token
                    type: boolean
                  commonNames:
                    description: Matches Certificates CNs
                    items:
                      type: string
                    type: array
                  countries:
                    description: Matches Country IDs (https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)
                    items:
                      type: string
                    type: array
                  emailDomains:
                    description: Matches specific email domains
                    items:
                      type: string
                    type: array
                  emails:
                    description: Matches specific email adresses
                    items:
                      type: string
                    type: array
                  everyone:
                    description: Allow Everyone; would always be a match
                    type: boolean
                  githubOrganizations:
                    description: Matches Github Organizations
                    items:
                      properties:
                        identityProviderId:
                          description: An Identity Provider UUID of a "github" type
                            Identity Provider (https://developers.cloudflare.com/api/resources/zero_trust/subresources/identity_providers/methods/get/)
                          type: string
                        name:
                          description: The name of the organization.
                          type: string
                        team:
                          description: The name of the team, if restricting to it.
                          type: string
                      required:
                      - identityProviderId
                      - name
                      type: object
                    type: array
                  googleGroups:
                    description: Matches Google Groups
                    items:
                      properties:
                        email:
                          description: Google group email
                          type: string
                        identityProviderId:
                          description: An Identity Provider UUID of a "google" type
                            Identity Provider (https://developers.cloudflare.com/api/resources/zero_trust/subresources/identity_providers/methods/get/)
                          type: string
                      required:
                      - email
                      - identityProviderId
                      type: object
                    type: array
                  ipRanges:
                    description: Matches IP CIDR blocks (https://www.ipaddressguide.com/cidr)
                    items:
                      type: string
                    type: array
                  loginMethods:
                    description: Matches on Identity Provider UUIDs (https://developers.cloudflare.com/api/resources/zero_trust/subresources/identity_providers/methods/get/)
                    items:
                      type: string
                    type: array
                  oktaGroups:
                    description: Matches Okta Groups
                    items:
                      properties:
                        identityProviderId:
                          description: An Identity Provider UUID of a "okta" type
                            Identity Provider (https://developers.cloudflare.com/api/resources/zero_trust/subresources/identity_providers/methods/get/)
                          type: string
                        name:
                          description: Name of the Okta Group
                          type: string
                      required:
                      - identityProviderId
                      - name
                      type: object
                    type: array
                  samlGroups:
                    description: Matches SAML Groups
                    items:
                      properties:
                        identityProviderId:
                          description: An Identity Provider UUID of a "saml" type
                            Identity Provider (https://developers.cloudflare.com/api/resources/zero_trust/subresources/identity_providers/methods/get/)
                          type: string
                        name:
                          description: Name of the SAML group
                          type: string
                        value:
                          description: Value of the SAML group
                          type: string
                      required:
                      - identityProviderId
                      - name
                      - value
                      type: object
                    type: array
                  serviceTokenRefs:
                    description: Would match access groups refs by {name} or {namespace/name}
                      of [CloudflareServiceToken]
                    items:
                      type: string
                    type: array
                  validCertificate:
                    description: Would be a match if using any valid certificate
                    type: boolean
                type: object
            required:
            - include
            - name
            type: object
          status:
            description: CloudflareAccessGroupStatus defines the observed state of
              CloudflareAccessGroup.
            properties:
              accessGroupId:
                description: AccessGroupID is the ID of the reference in Cloudflare
                type: string
              conditions:
                description: Conditions store the status conditions of the CloudflareAccessApplication
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: Creation timestamp of the resource in Cloudflare
                format: date-time
                type: string
              resolvedCfIds:
                properties:
                  exclude:
                    properties:
                      accessGroupRefCfIds:
                        items:
                          type: string
                        type: array
                      serviceTokenRefCfIds:
                        items:
                          type: string
                        type: array
                    type: object
                  include:
                    properties:
                      accessGroupRefCfIds:
                        items:
                          type: string
                        type: array
                      serviceTokenRefCfIds:
                        items:
                          type: string
                        type: array
                    type: object
                  require:
                    properties:
                      accessGroupRefCfIds:
                        items:
                          type: string
                        type: array
                      serviceTokenRefCfIds:
                        items:
                          type: string
                        type: array
                    type: object
                type: object
              updatedAt:
                description: Updated timestamp of the resource in Cloudflare
                format: date-time
                type: string
            type: object
        required:
        - metadata
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
